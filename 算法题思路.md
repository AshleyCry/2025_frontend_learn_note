### 最长连续序列

将数组转为set，遍历数组。如果有当前数-1的值，continue。

如果没有，记录连续序列长度。循环，如果存在当前数+1，计数+1，当前数+1

### 接雨水

计算前缀最大值数组和后缀最大值数组，每个位置的接水量为min（前缀，后缀）-height[i]

### 无重复字符最长字串

滑动窗口，记录set[r]，如果r重复，从l开始删除set元素直到set没有r，然后添加r

### 找到字符串中的字母异位词

维护两个26位数组，记录s和p中字幕出现数量

滑动窗口内两个数组对不上，l右移，sArr剪掉最左侧字母

### 和为K的子数组

前缀和数组求出来，然后转化为两数之和

### 滑动窗口最大值

造一个记录下标的队列，遍历数组元素。当队列不为空且队尾元素小于当前元素的时候，队尾出队，最后当前元素入队。

判断队头是否离开滑动窗口，如果是，队头出队

当下标>k-1的时候，记录队头对应元素，就是当前滑动窗口最大值

### 最小覆盖字串

构建res空数组，minLen，tMap，sMap。记录tMap为t每个字母出现次数

构建方法isMapOk，检查对于tMap中的每一项，是否都存在于sMap且sMap中的值不小于tMap中的值

滑动窗口i0与i，sMap添加i信息，当i0<=i且（tMap不存在s[i0]或者sMap[s[i0]]>tMap[s[i0]]），sMap去掉当前i0，i0++

如果符合条件，尝试更新minLen和res，然后sMap去掉当前i0，i0++

### 最大子数组和

维护一个当前最大和，初始为0

遍历数组，如果当前最大和小于0，则新的最大和为0+当前数，否则为之前最大和+当前数

比较res，更新

### 合并区间

维护res数组和last数组

将子数组按照第一个元素排序，然后遍历

如果当前数组第一个元素小于last第二个元素：

​	如果第二个元素小于last第二个元素，不做处理

​	如果第二个元素大于last第二个元素，将last第二个元素改为当前数组的第二个元素

如果当前数组第一个元素大于last第二个元素，last推入res，last更新为当前数组

最后将last推入res，返回res

### 轮转数组

先将整个数组反转

找到轮转截断点：k%length，截断点后两个子数组反转

### 除自身以外数组的乘积

维护两个数组pre和suf，pre[i]为0到i-1那些数的乘积，suf[i]为i+1到n那些数的乘积

为方便计算，pre[0]=suf[n]=1

然后每一位的结果就是pre[i] * suf[i]

### 缺失的第一个正整数

遍历数组，在每一位循环：

如果a[i] >= 1 && a[i]<= length 交换a[i]与a[a[i] - 1]

再遍历一遍，如果a[i]!==i + 1，返回i+1

否则返回length+1

### 矩阵置0

维护colSet，记录哪些列应该置为0。遍历矩阵，将每一行中0的列号记入set。如果一行里面出现了0，遍历完这行后将这行全部替换为0.

遍历列，如果列号在colSet中，则整列替换为0

### 螺旋矩阵

设置一个表示移动方向的list：[[0, 1], [1, 0], [0, -1], [-1, 0]]

res = []

m = matrix.length

n = matrix[0].length

当前行号row，当前列号col，当前方向索引directIndex，为0

从0到m*n，向前遍历：

​	将当前行号列号对应矩阵元素推入结果数组，然后将矩阵中这个位置置为null

​	根据directIndex计算仍然按照现在的方向，下一个行号列号是什么

​	如果预想的下一个行号列号超出范围，或者指向的元素为null，directIndex = (directIndex + 1) % 4.根据新方向算出新的行号列号

​	否则直接使用预想的行号列号

返回res

### 旋转图像

以左下到右上这根线为轴，互换两边的元素，然后在reverse

### 搜索二维矩阵

每次从右上角元素开始比较：

​	如果右上角元素大于target，把这一列移除搜索范围

​	如果右上角元素小于target，把这一行移除搜索范围

​	如果相等，则找到了

​	



### 相交链表

headA为起点，走到末尾，将所有遍历到的节点入set

headB往末尾走，如果当前节点在set中，返回这个相交节点

### 反转链表

新增prev=null, cur = head

遍历cur，记下next = cur.next。将cur改为指向prev，然后prev=cur，cur=next

返回prev

### 回文链表

找到中间节点：

slow, fast指向head，slow每次走一步，fast每次走两步。fast为null或者fast.next为null的时候slow就是中间节点

反转中间节点开始的链表

同时遍历头节点链表和中间节点链表，如果出现某个节点val不相同，返回false

否则返回true

### 环形链表

slow, fast指向head，slow每次走一步，fast每次走两步。如果发生slow === fast，就说明有环

### 环形链表II

（需要背下来）

找到slow和fast相遇点，然后另一个指针从head往后走，head和slow每次移动一步，相遇的点就是入环点

### 合并两个有序列表

（循环）

构建dummy节点，在list1和list2都没有走到末尾的时候，如果list2.val<=list1.val，pointer.next = list2，反之取list1。最后哪个还有剩余节点就再接上去。最后返回dummy.next

（递归）

如果list1空了返回list2，反之返回list1

如果list2.val<=list1.val，list2.next=mergeTwoLists(list1, list2.next)，然后返回list2

反之对list1做同样的操作

### 两数相加

构建dummy节点，维护一个sum。

当l1没到头或者l2没到头或者sum还有值的时候循环：

​	如果l1存在，sum+l1.val

​	如果l2存在，sum+l2.val

​	pointer.next指向新建节点sum % 10

​	pointer后移，sum=floor(sum/10)

返回dummy.next

### 两两交换链表中的节点

（方法1）

如果链表长度小于2，直接返回head

维护dummy，node0指向dummy，往后依次维护node1, node2, node3

当node2不为空的时候：

​	互换node1, node2指向

​	node0=node1，依次更新后面的node2,node3

返回dummy.next

（方法2）

算出链表长度，维护dummy，p0指向dummy

当长度>=2的时候，循环

​	pre=null, cur=p0.next

​	执行两次反转链表

​	新的p0设为p0.next，p0.next.next=cur，p0.next=pre，p0设为新的p0

​	长度-2

返回dummy.next

### K个一组反转链表

上一题的方法2，将2换为k

### 随机链表的复制

（方法1）

维护一个map，新建方法copyNode

copyNode：

​	如果map中有node对应的新节点，直接返回这个节点

​	如果没有，新建节点newNode,newNode放入map

​	newNode.val = node.val

​	newNode.next = node.next ? copyNode(node.next) : null

​	newNode.random= node.random? copyNode(node.random) : null

​	return newNode

维护dummy,pointer指向dummy

头节点不空的时候：

​	pointer.next = copyNode(head)

​	pointer = pointer.next

​	head = head.next

return dummy.next

（方法2）

将链表每个节点复制一份放在源节点后面，串成一个两倍长的链表（新节点先不管random）

再从头遍历节点，cur.next.random = cur.random.next

维护dummy节点，再遍历一遍将两个链表摘出来

返回dummy.next

### 排序列表(背)

先写一个split方法，方法可以将一个链表从中间截断为两个链表，然后返回后半段的头节点

写一个merge方法，方法可以按照升序合并两个链表

sort方法中，如果链表为空或者只有一个节点则返回head，然后分割链表获得第二个头head2；

对head递归调用sort方法，对head2递归调用sort方法

将head和head2合并

返回合并后的头节点

方法2

把每个节点放进数组，根据val排序，然后在组成链表

### 合并K个升序列表

编写一个升序合并两个链表的方法

递归数组dfs(i, j)：

​	如果j-i=0，返回null

​	如果j-i=1，返回lists[i]

​	mid=Math.floor(i+j)

​	left=dfs(i, mid)

​	right=dfs(mid, j)

​	合并两个数组

返回dfs(0, length)

### LRU

双向链表，只维护一个dummy即可

### 对称二叉树

一个左右对比函数isSame，如果!left || !right，返回left === null && right === null

否则返回left.val === right.val && isSame(left.left, right,right) && isSame(left.right, right.left)

对root.left, root.right使用isSame

### 二叉树直径

维护res，递归方法dfs

dfs中：

​	如果当前节点为空，返回-1

​	递归计算左长度leftLen和右长度rightLen：（dfs(node.left) + 1），这里的+1就是子节点到当前节点的长度

​	比较一下如果从当前节点拐弯的话，（路径长度为左子树长度+右子树长度）会不会比现在的res大，大的话更新res

​	返回左右子树长度最大的那个

### 层次遍历

如果树是空的，返回空数组

否则维护res[], 队列queue，默认里面带root节点

当队列不为空：

​	维护当前层级结果curLevel[]，当前层级长度=queue.length

​	循环层级长度次，每次队头弹出一个节点，将值放入curLevel，然后有子节点的话将子节点放入队列

​	将curLevel添加到res中

返回res

### 有序数组转为二叉搜索树

如果数组为空返回null

否则取数组中点，用中点的值新建树节点

节点左子树为中点前数组的递归，右子树为中点后数组的递归

返回中点创建的节点

### 验证二叉搜索树

二叉搜索树每个节点都要符合一个开区间，初始情况下，根节点的区间为（-Inf, Inf）

建立dfs方法，参数为当前节点root、当前节点需要大于的值left，当前节点需要小于的值right

如果root为空，dfs返回true

否则dfs返回root.val > left && root.val < right && dfs(root.left, left, root.val) && dfs(root.right, root.val, right)

返回dfs(root, -Infinity, Infinity)

### 

### 

### 

### 

### 二叉搜索树中第k小的元素

中序遍历二叉搜索树，结果是一个递增序列

对树进行中序遍历，如果当前节点为空或者k=0，直接返回

遍历左子树

k减1

如果k=0，返回当前节点的值

遍历右子树

### 二叉树右视图

维护dfs(node, depth)

如果node为空直接返回，如果res.length < depth将当前节点插入res

dfs(node.right, depth+1)

dfs(node.left, depth + 1)

执行dfs(root, 1)

返回res

### 二叉树展开为链表

方法1：

先序遍历倒过来就是右左中

维护head=null

dfs中：

​	!node直接return

​	dfs左子树

​	dfs右子树

​	node.left = null node.right = head head = node

### 从前序与中序遍历中构建二叉树

先构建一个map，存储中序遍历中每个值对应的索引

dfs(preLeft, preRight, inLeft, inRight):

​	如果preLeft>preRight，说明节点为空，返回null

​	通过当前节点再inorder中的位置，计算出左子树、右子树的preLeft, preRight, inLeft, inRight

​	dfs构建完左右子树后，构建当前节点

return dfs(0, len, 0, len)

### 路径之和III

类似于两数之和，维护一个记录和为x的路径有多少个的map

map.set(0, 1)

先序遍历数组，dfs(node, sum)：

​	如果node为空直接返回

​	s += node.val

​	寻找map中target - s有多少个，加到res

​	map中s数量加1

​	dfs(left, s)

​	dfs(right, s)

​	map中s数量-1

### 二叉树的最近公共祖先

如果当前节点为空返回null

如果当前节点为p或者为q，返回当前节点

遍历左子树，找到左子树的祖先候选left

遍历右子树，找到右子树的祖先候选right

如果left和right都存在，返回root

如果left存在返回left，right存在返回right

### 二叉搜索树的最近公共祖先

如果当前节点为空返回null

如果p,q的值都小于当前节点，返回递归左子树的结果

如果p,q的值都大于当前节点，返回递归右子树的结果

否则返回root

### 二叉树的最大路径和

res=-Infinity

dfs(node):

​	如果node为空，返回0

​	递归计算left=dfs(node.left)

​	递归计算right=dfs(node.right)

​	假设最大路径和为在node处拐弯的路径，更新res为Math.max(res, left + right + node.val)

​	计算左右子树最大的值与node.val的和，如果这个和小于0，则最大路径和与当前节点无关，返回0

### 岛屿数量

维护一个dfs(i,j)方法，当i,j不越界并且当前格的值不为1的时候什么都不做，否则将格子改为2，然后递归上下左右四个格子

循环遍历矩阵，如果发现1，计数加1，执行dfs(i,j)

### 腐烂的橘子

首先统计出有多少新鲜橘子，多少腐烂橘子，维护腐烂坐标列表robList

维护腐烂方法rob(i, j)，如果i,j越界或者当前格子不是新鲜橘子，return；否则当前格子变成'2'，新鲜数-1，当前腐烂橘子加入robList

当还存在新鲜橘子或者腐烂橘子的情况下，循环：

​	记录当前腐烂橘子坐标列表oldRobList

​	清空腐烂橘子坐标列表

​	时间+1

​	对oldRobList中的每一个腐烂橘子，上下左右四个格子执行rob

如果最后还有新鲜橘子，返回-1，否则返回res

### 课程表

本质是看一个有向图里面是不是有环

根据prerequisites构建一个有向图，每一个[a,b]就是一个b->a的边

维护一个color数组，长度为numCourses，每一位0代表没访问过，1代表正在dfs递归中，2代表访问完成没有环

维护dfs(x)方法，先将color[x]改为1，然后对图中x的每条边遍历，边的另一端color[y]为1，或者边的另一端为0且dfs(y)为true，返回true；否则color[x]=2，返回false

遍历color，如果color[i]=0且dfs(i)=true，返回false。否则返回true

### 实现前缀树

构建一个26叉树，每个节点有sons(长度26的数组，每一项为一个节点)，isEnd（记录当前节点是否为某个词的结尾）

insert方法：遍历字符串，维护cur=root，往下走如果发现某个节点不在树里面就new出来，最后把最后的cur的isEnd设为true

find方法：cur=root，遍历字符串，遍历树，如果路径不存在返回0，如果路径存在且最后的cur为1就是部分匹配，2就是完全匹配
